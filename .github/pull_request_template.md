<!-- Thanks for contributing to fox! Keep this short. -->

## What & why

<!-- What does this change do, and why? Link any related issue: Closes #123 -->

## How was it tested?

<!-- e.g. `make ci` (FOX_SKIP_LLAMA), a real Docker build, `fox probe <model>`,
     manual requests against a running server, a new golden/unit test. -->

## Checklist

- [ ] `make ci` passes locally (`cargo fmt --check`, `clippy -D warnings`, tests)
- [ ] Docs updated if behavior/flags/endpoints changed (`docs/`, `CHANGELOG.md`)
- [ ] Targets the `develop` branch
